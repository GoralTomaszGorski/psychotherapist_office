spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gabinet
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 8557
  liquibase:
    contexts: prod
    change-log: classpath:db/changelog/master2.xml

  jpa:
    hibernate:
      ddl-auto: update